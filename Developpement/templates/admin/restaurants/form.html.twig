{% extends 'base.html.twig' %}

{% block title %}{{ title }} — {{ parent() }}{% endblock %}

{% block body %}
    <h1>{{ title }}</h1>
    <p><a href="{{ path('app_admin_restaurants_index') }}">← Liste des établissements</a></p>

    {{ form_start(form) }}
        {{ form_row(form.name) }}
        <p><button type="submit">Enregistrer</button></p>
    {{ form_end(form) }}
{% endblock %}

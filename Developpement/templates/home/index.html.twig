{% extends 'base.html.twig' %}

{% block title %}Accueil — {{ parent() }}{% endblock %}

{% block body %}
    <h1>Accueil</h1>
    <p>Réservation en ligne pour un restaurant (projet école). Interface volontairement brute pour les tests.</p>
    <ul>
        <li><a href="{{ path('app_reserver') }}">Réserver une table</a></li>
        <li><a href="{{ path('app_login') }}">Connexion</a> (restaurateurs et admin plateforme) — <a href="{{ path('app_register') }}">inscription restaurateur</a></li>
        <li>Espace après connexion : <code>/restaurateur</code> (tables) ou <code>/admin</code> (fiches restaurants) selon votre rôle</li>
    </ul>
{% endblock %}
